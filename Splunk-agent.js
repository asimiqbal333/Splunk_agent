export default function handler(req, res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','POST, OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type');if(req.method==='OPTIONS')return res.status(200).end();if(req.method!=='POST')return res.status(405).json({error:'Only POST'});const{alert_id,alert_text,source,user_age}=req.body;if(!alert_id||!alert_text||!source||!user_age)return res.status(400).json({error:'Missing fields'});const text=alert_text.toLowerCase();const isScam=text.includes('crypto')||text.includes('usdt')||text.includes('prize')||text.includes('claim')||text.includes('wallet');const isMinor=parseInt(user_age)<18;const risk_level=isScam&&isMinor?'High':'Safe';const reason=risk_level==='High'?'GenZ scam targeting minor - AU eSafety threat':'No scam detected';const suggested_action=risk_level==='High'?'Block and report to eSafety.gov.au':'Monitor';return res.status(200).json({alert_id,risk_level,reason,suggested_action,platform:source,timestamp:new Date().toISOString()});}
